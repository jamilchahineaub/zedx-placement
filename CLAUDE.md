# ZED-X Dual Camera Placement Experiment
# Isaac Sim 5.1 + ZED SDK 5.3.1

## Repo state
Some files are already written and tested. Do not regenerate files marked done.
Done: config/experiment.yaml, config/machine.laptop.yaml,
zed/zed360_template.json, zed/generate_fusion_template.py, zed/make_fusion_config.py

## Hard environment facts — never guess these
- Isaac Python:         /home/jimmy/isaacsim/python.sh
- ZED Python:           python3 (system, pyzed installed)
- ZED ext folder:       /home/jimmy/zed-isaac-sim/exts
- ZED SDK version:      5.3.1
- ZED Helper node type: sl.sensor.camera.ZED_Camera  (typeVersion 2; uiName "ZED Camera Helper")
                          # 2-cam port-based node. No serial field — cams differ by
                          # streamingPort(30000/30002)+cameraPrim. Attrs in experiment.yaml
                          # under zed_helper_attrs. Stream cfg: HD1080 @ 30fps (zed_stream).
- Reference scene:      /home/jimmy/Desktop/jimmyNYUL/test.usd (DO NOT MODIFY)
- Repo root:            /home/jimmy/zedx-placement

## Two runtimes — never mix
- isaac/   → /home/jimmy/isaacsim/python.sh
             imports: pxr, omni, isaacsim
- zed/     → python3
             imports: pyzed.sl only
- analysis/, sweep.py → python3, no special imports

## Coordinate system — non-negotiable (CORRECTED 2026-06-11)
Two frames. Do not mix them up.

Isaac side (scene_builder, gt_logger, camera_rig): RIGHT_HANDED_Z_UP, metres.

ZED SDK side (pyzed init, fusion config poses): RIGHT_HANDED_Y_UP, metres.
  This matches the official multi-camera body-tracking sample
  (/usr/local/zed/samples/body tracking/multi-camera/python/fused_cameras.py),
  which reads BOTH capture and fusion with sl.COORDINATE_SYSTEM.RIGHT_HANDED_Y_UP.
  In ALL pyzed code (zed_single.py, zed_fusion.py):
      init.coordinate_system = sl.COORDINATE_SYSTEM.RIGHT_HANDED_Y_UP
      init.coordinate_units  = sl.UNIT.METER

make_fusion_config.py converts Isaac Z-up poses for the fusion 'pose' (4x4).
CORRECTED 2026-06-11 (empirically, from real fused output — the old P R Pᵀ rule
and the zed-isaac-sim convert script were both wrong):
  1. The RUNTIME pose fusion applies is (P @ R, P @ t) with P(x,y,z)=(-y,-z,x).
     Only the WORLD side is permuted — the ZED Y-up camera frame already matches
     R's camera-side axes (columns [right, up, -forward]), so conjugation
     P R Pᵀ wrongly permutes the camera axes (put fused bodies metres off).
  2. The FILE stores poses in the ZED IMAGE frame (x right, y DOWN, z FORWARD);
     read_fusion_configuration_file converts file->runtime by conjugating with
     D = diag(1,-1,-1). So the file must contain:
         file_R = D (P R) D        file_t = D P t = (-y, z, -x)
NOTE: camera_rig.rotation_matrix_from_look_at returns a row-basis (det -1) matrix —
fine for direction math, but make_fusion_config.proper_rotation_world_from_cam()
rebuilds a proper (det +1) rotation before conversion.

## Headless flag
laptop → headless: false  (user watches viewport, sees cameras moving)
4090   → headless: true   (batch mode)
Always read from config/machine.{name}.yaml. Never hardcode.

## Camera geometry
Both cameras on a ring around subject S = (0,0,0).
Same height and radius for both cameras in every layout.
Cam A at azimuth 0 degrees, cam B at (0 + relative_azimuth) degrees.
Cameras aim at (0, 0, aim_height_m) — hip height, NOT the floor.
Tilt must be less than max_tilt_deg from experiment.yaml or layout is skipped.
Both camera poses computed by camera_rig.py, never hardcoded.

## ZED fusion config
Template: zed/zed360_template.json (already exists, do not regenerate)
Per-layout config: generated by zed/make_fusion_config.py (already written)
DO NOT run ZED360. It cannot connect to Isaac streams. Not needed.

## evaluate_layout() is the sweep boundary
isaac/camera_rig.py must expose:
  evaluate_layout(h, r, rel_az_deg, subject_pos, cfg) -> dict of all metrics
sweep.py calls ONLY this function. Search strategy lives only in sweep.py.
This stub must exist from the start even before it is implemented.

## Sentinel lines (sweep.py keys off these exactly)
STREAMING_STARTED
EPISODE_DONE
FUSION_READY
RUN_FAILED <reason>
GEO_SKIPPED <layout_id> <reason>

## Metrics every results.csv row must have
h_m, r_m, rel_az_deg, tilt_deg, convergence_angle_deg,
subject_pos_name, mpjpe_mm, pck30, pck50,
detection_coverage, joint_visibility_cam_a, joint_visibility_cam_b,
joint_visibility_either, joint_visibility_both,
unique_contribution_cam_b, jitter_variance, id_drops

## Hard rules
- Never hardcode any path. Read from config/machine.{name}.yaml.
- pyzed members drift. Before writing any pyzed code, read the installed
  samples at /usr/local/zed/samples/ and mirror exact member names.
- Isaac and USD APIs: grep /home/jimmy/isaacsim/exts/ before writing.
- Never write to results/ except appending.
- analysis/ must have pytest unit tests. Run them after every edit.
- Ask before adding any new pip dependency.

## Definition of done per phase
P1 node_type: grep of test.usd returns ZED node type string, filled into this file
P2 scene:     run_episode.py opens Isaac GUI, room visible, two camera prims
              at correct positions, character in scene, no red errors
P3 stream:    ZED body_tracking.py sample shows skeleton from port 30000
P4 gt:        gt_logger writes CSV, pelvis z between 0.8-1.2m standing
P5 fusion:    zed_fusion.py writes fused rows using make_fusion_config.py output
P6 metrics:   MPJPE in 20-200mm band, tests green
P7 sweep:     3-layout mini-sweep completes, results.csv has 3 rows
